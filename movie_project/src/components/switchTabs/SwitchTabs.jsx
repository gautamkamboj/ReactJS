import React, { useState } from "react";

import "./style.scss";

const SwitchTabs = ({ data, onTabChange }) => {

    return (
        <div >
            
        </div>
    );
};

export default SwitchTabs;
